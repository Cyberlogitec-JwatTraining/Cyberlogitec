<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.thinhdo.mapper.StudentMapper">
    <insert id="insertStudent" parameterType="Student" >
        INSERT INTO STUDENT (ID, NAME, BRANCH, PERCENTAGE, PHONE, EMAIL ) VALUES (#{id}, #{name}, #{branch}, #{percentage}, #{phone}, #{email});
    </insert>

    <update id="updateStudent" parameterType="Student">
        UPDATE STUDENT SET EMAIL = #{email}, NAME = #{name}, BRANCH = #{branch}, PERCENTAGE = #{percentage}, PHONE = #{phone} WHERE ID = #{id};
    </update>
    
    <delete id="deleteStudentById" parameterType="int">
        DELETE from STUDENT WHERE ID = #{id};
    </delete>
    
    <select id="selectAllStudent" resultMap="result">
        SELECT * FROM STUDENT;
    </select>
    
    <select id="selectStudentById" parameterType="int" resultType="Student">
        SELECT * FROM STUDENT WHERE ID = #{id};
    </select>
    
    <!-- Return List of student contains the name -->
    <select id="selectStudentsByName" parameterType="java.lang.String" resultMap="result">
    	SELECT * FROM STUDENT 
		<if test="'#{findName}'!=null">
			WHERE NAME LIKE CONCAT('%', #{findName} , '%')
		</if>
   	</select>
    
    <select id="selectStudentByPhoneOrName" resultType="Student">
    	SELECT * FROM STUDENT WHERE 1=1 
    	<choose>
    		<when test="name!=''">
    			AND NAME LIKE #{name}
    		</when>
    		<when test="phone!=0">
    			AND PHONE = #{phone} 
    		</when>
    	</choose>
    	limit 1
    </select>
    
    <!-- Mapping whether the name between properties & columns are differenced -->
    <!-- Return ResulSet -->
    <resultMap id = "result" type = "Student">
       <result property = "id" column = "ID"/>
       <result property = "name" column = "NAME"/>
       <result property = "branch" column = "BRANCH"/>
       <result property = "percentage" column = "PERCENTAGE"/>
       <result property = "phone" column = "PHONE"/>
       <result property = "email" column = "EMAIL"/>
    </resultMap>
</mapper>